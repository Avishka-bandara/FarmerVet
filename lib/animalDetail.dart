import 'package:flutter/material.dart';

class AnimalDetail extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Animal Detail'),
      ),
      body: Center(
        child: Text('Animal Detail Screen'),
      ),
    );
  }
}
